import { TrimFormControlDirective } from './trim-form-control.directive';

describe('TrimFormControlDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimFormControlDirective();
    expect(directive).toBeTruthy();
  });
});
